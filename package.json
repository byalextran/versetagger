{
  "name": "versetagger",
  "version": "1.0.0",
  "description": "VerseTagger saves time by automatically linking verses on your website. Readers hover over references like \"Romans 8:28\" to see the full text instantly.",
  "main": "dist/versetagger.js",
  "module": "dist/versetagger.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --dev",
    "watch": "node build.js --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest watch",
    "release:patch": "npm run build && npm version patch && npm publish && git push --follow-tags",
    "release:minor": "npm run build && npm version minor && npm publish && git push --follow-tags",
    "release:major": "npm run build && npm version major && npm publish && git push --follow-tags"
  },
  "keywords": [
    "bible",
    "scripture",
    "verse",
    "youversion",
    "modal",
    "bible.com"
  ],
  "author": "Alex Tran",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@types/node": "^25.0.3",
    "@vitest/coverage-v8": "^4.0.16",
    "@vitest/ui": "^4.0.16",
    "dotenv": "^17.2.3",
    "esbuild": "^0.20.0",
    "jsdom": "^27.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.16"
  }
}
