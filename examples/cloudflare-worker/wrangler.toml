# Cloudflare Workers configuration for VerseTagger proxy
# https://developers.cloudflare.com/workers/wrangler/configuration/

name = "versetagger-proxy"
main = "worker.ts"
compatibility_date = "2024-01-01"

# Worker settings
[build]
command = ""

# Optional: Enable KV for caching verse responses
# Uncomment and set up KV namespace for better performance
# [[kv_namespaces]]
# binding = "VERSE_CACHE"
# id = "your_kv_namespace_id"
# preview_id = "your_preview_kv_namespace_id"

# Route configuration (example)
# [env.production]
# routes = [
#   { pattern = "yourdomain.com/api/verses", zone_name = "yourdomain.com" }
# ]

# Secrets (set via CLI):
# $ wrangler secret put YOUVERSION_API_KEY
